---
name: viral-video-editing
description: 为竖屏或横版口播制作高信息密度知识流视频。用于剪辑口播、生成带原声预览或正式成片、根据语义加入信息卡片和科技感动效、保留用户字幕与配乐、制作分镜和修复音画字幕同步；需要录制或润色产品操作、网页和工作台演示时，使用 Recordly 制作可插入的录屏素材。
---

# 爆款剪辑 Skill

> 风格：高信息密度知识流口播

适用：竖屏知识口播、工具/产品/方法论分享。目标是让观众静音时仍能通过左侧信息层快速理解当前论点。

## 工作区风格包调用提醒

- 开始任何视频剪辑、口播成片或工作台演示任务前，先检查当前工作区是否存在 `video_project/workbench_tutorial_style_package/`。
- 如果存在，先读取其中的 `README.md`、`style-guide.md` 和 `reference-index.md`，将其作为本次任务的默认风格，并向用户提醒：`已检测到工作区风格包，将沿用已确认的画面、画中画、截图弹出和动效规则。`
- 风格包不是新的 Skill，不需要单独调用；它由本 Skill 在工作区内自动复用。当前用户的新需求优先级高于风格包，若发生冲突，应在分镜表或交付说明中记录调整原因。
- 若工作区没有风格包，按本 Skill 的内置视觉规则执行；在阶段 F 完成确认后，可按下方规范创建或更新工作区风格包。

## 已确认的视觉规则

### 构图与安全区

- 原视频不裁切、不拉伸、不强制把人物移到右侧。
- 优先使用左侧空白区；若人物居中，则把信息带收在眉眼以上，绝不遮住双眼、嘴部或底部字幕。
- 底部 25% 默认保留给用户自己的字幕；不新增或覆盖字幕。
- 同屏最多一个主结果卡，加两到三个轻量辅助元素；新逻辑组开始时，上一组先整体退出。

### 信息层级

1. **主题标签**：左上固定。小竖线 + 英文大写分类 + 中文主题。例如：`│ TOPIC / 01`、`工作台为什么被问爆`。
2. **核心观点**：白色小标题配一个超大号蓝色粗体关键词；关键词必须完整收在半透明黑色卡片内。
3. **分点列举**：黑底圆角条搭配不同颜色的圆形图标，按 0.22–0.32 秒间隔依次进入。
4. **他人观点/普遍现象**：微信私信式评论卡，包含抽象头像、来源小标签和一句提炼文案。
5. **数据/结果**：大号绿色数字或绿色关键词，用于结果、增长、完成和结论；不要滥用。

### 颜色与字体

- 底色：近黑蓝 `#060D19`，半透明约 82%–88%。
- 主强调：蓝 `#39B2FF`；结果强调：绿 `#45EBB3`；正文：偏白 `#F5F9FF`；辅助：灰蓝 `#B1C0D2`。
- 卡片：圆角 16–20px，细描边、顶部 3px 颜色线、轻微黑色阴影。
- 中文字体优先 Noto Sans SC / 思源黑体；关键词使用粗体，信息正文使用中等字重。

### 动效与节奏

- 所有元素使用短促、克制的弹性进入：0.18–0.28 秒，透明度 + 小位移 + 96%→100% 缩放。
- 关键词在重音词开始后的 0–220ms 内触发；不要等待整句话说完。
- 同一逻辑组使用“输入 → 关系 → 结果”的出现顺序；元素可短暂累积，结论说完后整组在 0.20–0.28 秒内淡出。
- 结果卡不应停留到预览结尾：预留 1.5–2 秒干净画面作为下一论点或停顿。
- 不要持续抖动、闪烁、扫描线、随机飞字或重复同一种卡片入场。

## 语义到组件的映射

| 口播作用 | 使用组件 | 文案方式 |
|---|---|---|
| 核心观点 | 大关键词卡 | 提炼为 4–10 字结论 |
| 分点/分类 | 图标条 stagger | 例如“第一类 / 第二类 / 第三类” |
| 他人疑问 | 私信/评论卡 | 使用提问句，不照抄整段字幕 |
| 数据/比例 | 巨型数字卡 | 仅显示数字和 2–6 字结论 |
| 方法/流程 | 节点 + 箭头 + 结果卡 | 输入 → 处理 → 输出 |
| 转折/纠错 | 对比符号或状态切换 | 例如“照抄 ≠ 复刻” |

## 已验证的样片经验

- 人物居中时，横向大评论卡必须放在眼睛上方；此前放在眼睛位置会破坏观看体验。
- 超大关键词必须在设计阶段按真实字体测量，不能让字越出卡片底边。
- 结论组过长会显得“卡片挂在屏幕上”；本片将第二组在 12.92 秒开始退出，效果更干净。
- 原视频的音频应单独映射并重新编码为 AAC，避免部分播放器对复制音轨兼容不佳。

## 下次直接使用的启动提示词

```text
请按《高信息密度知识流口播风格包》为我制作一条竖屏口播预览，不要直接制作正式成片。

输入：我会提供原始口播视频、可选口播稿、已有字幕、配乐和参考图。
输出：先给 15–20 秒、720×1280、30fps、H.264 + AAC 的带声音预览；原视频音频必须保留并重新编码为 AAC。

规则：
1. 先从当前源视频生成词级时间轴，并按完整论证单元分组，不按标点机械切卡。
2. 每组按“输入 → 关系 → 结果”出现；信息卡是语义总结，不照抄字幕。
3. 左上常驻小标签：小竖线 + 英文大写分类 + 中文主题。
4. 核心观点使用白色短句 + 超大蓝色粗体关键词；数据/结果使用超大绿色数字或关键词。
5. 分点使用不同颜色圆形图标的黑底圆角条，按 0.22–0.32 秒 stagger 进入；他人观点使用带头像的评论卡。
6. 人物、眼睛、嘴部、手势、底部现有字幕是保护区。人物居中时，把信息组件收在眉眼以上或侧边空白区；不得裁切、拉伸、遮住脸或新增字幕层。
7. 每组结束后整体在 0.20–0.28 秒淡出，留出节奏停顿；不堆叠、不持续抖动、不做廉价扫描线或随机飞字。
8. 渲染前先输出关键帧；渲染后抽取每组核心帧验证文字没有越出卡片、眼睛嘴部和字幕没有被遮住、并确认 AAC 音轨可播放。
```

## 本次参考与产物

- 示例流程与分镜：[workbench-tutorial 示例](examples/workbench-tutorial/README.md)
- 语义弹窗时间轴：[semantic-popup-timeline.csv](examples/workbench-tutorial/timeline/semantic-popup-timeline.csv)

## Remotion 五阶段流程补充

本节补齐“风格校准 -> 时间轴/逻辑 -> 关键帧 -> 审核预览 -> 定点修改 -> 风格资产”的确认门禁。它与上面的视觉规则合并使用，不替换已有安全区和组件约束。

### 可选的分阶段提示词

保留上方“下次直接使用的启动提示词”作为默认的一键预览入口，不改变其输出和使用方式。用户需要以 Remotion 做完整的“风格校准 → 时间轴 → 关键帧 → 审核 → 定点修改 → 风格沉淀”时，读取 [references/remotion-prompt-workflow.md](references/remotion-prompt-workflow.md)，按顺序使用其中的 5 个提示词。不得跳过确认门禁，也不得将审核预览当作最终成片。

使用这些提示词时：

- 无参考截图时，跳过提示词 1，但仍先输出本片的构图、安全区、包装规则和待确认项。
- 用户只要求 15–20 秒预览时，执行提示词 1–3 后停止；不要主动进入正式成片或风格资产沉淀。
- 用户已确认预览且要求修改时，使用提示词 4；只改明确列出的时间段和对象。
- 用户明确确认正式成片后，使用原始最高质量素材重渲染，再执行媒体参数核验；确认完成后才使用提示词 5。

### 阶段 A：素材与风格校准

先锁定原始最高画质视频、真实媒体参数、口播稿/自动转写、参考截图和可选录屏。参考截图只用于提取构图、字体、颜色、字幕层级、卡片和节奏，不复制品牌元素。输出风格拆解表、包装规则、禁止项和待确认项；没有完成确认前不写 Remotion 组件。

### 阶段 B：字幕、词级时间轴与逻辑组

字幕必须同时输出校正后的 SRT 与 Remotion Caption 兼容 JSON。每条 JSON 至少包含 text、startMs、endMs、timestampMs、confidence。关键词使用真实词级起止时间，不用整句起点代替。按完整论证单元拆分逻辑组，记录每组的传播作用、视觉重点、组件和出入场方式；字幕记录“说了什么”，信息卡只保留“观众要记住什么”。

### 阶段 C：静态关键帧门禁

先生成开场、每个主要逻辑段、信息量最大画面和结尾关键帧。检查输入、关系、结果是否按顺序出现，文字是否越界，卡片是否遮挡人物保护区。关键帧未通过时停止，不渲染动态预览。

### 阶段 D：动态审核版

关键帧通过后再输出 1280x720、30fps、H.264 + AAC 的 15-20 秒审核版。Remotion 动画必须由 useCurrentFrame() 驱动，使用 Sequence + premountFor、interpolate 和显式 easing；文字先测量再布局。逐组检查字幕同步、关键词触发、元素累积、整组退出和音轨。审核版不能放大冒充最终母版。

### 阶段 E：定点修改与最终渲染

问题按“时间点 + 对象 + 当前问题 + 目标状态”描述，一轮只改相关问题。修改后先抽取受影响时间点前后 2-3 秒关键帧/短片段；若会影响已确认内容，先说明影响。确认后从原始素材重新渲染，并用 ffprobe 核对分辨率、时长、帧率、编码和音轨。

## Recordly 录屏演示模块

仅当用户需要录制或优化产品操作、网页、工作台或软件演示时使用。Recordly 是桌面录屏编辑器，不是 Codex 原生 skill；它负责生成录屏素材，知识流字幕、信息卡和最终合成仍由本 skill 的既有流程负责。

1. 启动前检查 `%LOCALAPPDATA%\Programs\recordly\Recordly.exe` 是否存在；存在时启动应用。若不存在，说明需要安装官方 Recordly Windows 发布包，不用源码构建替代发布包。
2. 默认录制单个目标窗口，避免录入通知、桌面图标和无关隐私信息。根据用户要求启用麦克风和/或系统音频；录制前确认音频电平有响应。
3. 在 Recordly 中仅修剪空白段、突出真实操作区域的自动/手动缩放、平滑光标与必要的点击反馈。画中画可用于演示讲解；不要让缩放或光标特效遮挡按钮、数据和关键文本。
4. 导出 MP4，保持源录制画幅和帧率。先实际播放检查画面、光标、缩放边界和音频，再将导出的原始录屏作为项目素材；不要把低分辨率预览作为后续合成源。
5. 插入知识流成片时，录屏使用 `contain` 保留完整界面，默认静音录屏自身音轨以原口播音频为主；用户明确要求演示原声时才混入录屏音频并重新核对同步。

录屏入场和结束后，恢复常规语义卡片节奏。不要改变用户已确认的字幕、配乐、主口播画面或主音轨。

### 阶段 F：风格资产沉淀

每次确认完成后保存 style-guide.md、style-prompt.txt、reference-index.md、lessons-learned.md 和下一期启动提示词。若当前工作区已有 `video_project/workbench_tutorial_style_package/`，优先更新该目录；没有时再创建独立风格包。记录保留规则、放弃版式、失败案例、转写修正和仍需人工确认的内容；不要把本期标题、台词、时间点或绝对路径写进通用提示词。
